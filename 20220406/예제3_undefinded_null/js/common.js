"use strict"

// undefinded test

// 출력 테스트
var variable;
console.log(typeof(variable));  //undefinded 자료형은 변수값을 선언만 하고 초기화를 하지 않은 상태

// null test
var nullTest = null;  //할당된 값이 없다는 것을 표현할때 사용
console.log(typeof(nullTest));  //null 자료형은 객체(object)타입, 메로리가 존재한다
